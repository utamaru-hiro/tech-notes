{
  "name": "tech-notes",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "type-check": "tsc --noEmit --project tsconfig.json && npm run type-check --workspace=apps/web"
  }
}
